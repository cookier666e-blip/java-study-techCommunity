---
alwaysApply: true
description: 前端页面交互规范
---

# 前端页面交互规范 (frontend-page-interaction-spec)

## 描述
本文档描述包含列表、新增、编辑、详情、删除页面之间的交互规范，统一前端页面交互行为，确保用户体验一致性。

---

## 1. 数据刷新规范 (data-refresh-spec)

### 1.1 禁止整页刷新
- **所有页面刷新只能刷新数据，不能通过 `window.reload` 或类似方式刷新整个页面**
- 列表数据更新应通过重新调用接口获取最新数据，然后更新组件状态
- 局部数据更新应精准更新对应状态，避免不必要的全量刷新

### 1.2 数据刷新方式
- 使用 React 状态管理（useState、useReducer）更新数据
- 使用自定义 Hooks 封装数据获取逻辑
- 通过回调函数通知父组件刷新数据

---

## 2. 新增/编辑页面交互规范 (create-edit-interaction-spec)

新增/编辑页面支持**抽屉**和**页面**两种形式，默认为抽屉形式。

### 2.1 抽屉形式 (drawer-mode)

#### 参数传递
- 通过 `props` 参数或 `open` 方法携带 `id` 等参数
- 支持参数：
    - `id`: 编辑时的数据ID
    - `type`: 操作类型（'create' | 'edit'）
    - 其他业务参数

#### 数据回填流程
1. 打开抽屉时传递参数
2. 如果是编辑模式（有id），调用接口获取默认值
3. 使用获取的数据回填表单初始值
4. 用户修改后点击确定提交

#### 提交后处理
1. 调用提交接口
2. 关闭抽屉
3. **刷新列表数据进行数据更新显示**

### 2.2 页面形式 (page-mode)

#### 参数传递
- 通过**路由携带参数**跳转页面（新开窗口）
- URL 参数示例：`/device/edit?id=123&type=edit`

#### 获取路由参数
- 使用路由文档提供的方式获取路由参数
- 从 URL 中解析 `id`、`type` 等参数

#### 数据回填流程
1. 页面加载时从路由获取参数
2. 如果是编辑模式（有id），调用接口获取数据
3. 使用获取的数据回填表单初始值
4. 用户修改后点击确定提交

#### 提交后处理
1. 调用提交接口
2. **跳转回列表页**
3. **刷新列表数据进行数据更新显示**

---

## 3. 详情页面交互规范 (detail-interaction-spec)

### 3.1 跳转方式
- 通过列表的**查看按钮**或其他跳转按钮跳转详情页面
- **必须新开窗口**打开详情页
- 跳转需遵循 [frontend-architecture-spec.md](./frontend-architecture-spec.md) 中的路由规范

### 3.2 参数传递
- 通过路由携带参数，如：`/device/detail?id=123`
- 使用路由文档提供的方式获取路由参数

### 3.3 数据展示流程
1. 页面加载时从路由获取 `id` 参数
2. 调用接口获取详情数据
3. 渲染页面展示数据

---

## 4. 删除操作交互规范 (delete-interaction-spec)

### 4.1 二次确认
- **点击删除按钮后必须有 Confirm 二次确认**
- 确认文案应清晰说明删除后果
- 支持取消操作

### 4.2 删除后处理
1. 用户确认删除
2. 调用删除接口
3. **刷新列表数据**（不能整页刷新）

---

## 5. 列表操作按钮规范 (list-action-spec)

### 5.1 操作后刷新规则
**所有在列表页进行操作列表数据的交互按钮，在提交接口后都需要刷新列表。**

包括但不限于：
- 删除操作
- 状态变更（启用/禁用）
- 批量操作
- 排序调整
- 其他数据修改操作

### 5.2 刷新方式
- 调用 `refreshList()` 方法重新获取数据
- 更新列表状态，保持当前分页和查询条件
- 显示操作成功提示

---

## 6. 交互流程图 (interaction-flow)

```
┌─────────────────────────────────────────────────────────────┐
│                         列表页                               │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │  新增   │  │  编辑   │  │  查看   │  │  删除   │        │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘        │
│       │            │            │            │              │
│       ▼            ▼            ▼            ▼              │
│   ┌───────┐   ┌───────┐   ┌───────┐   ┌──────────┐         │
│   │ 抽屉  │   │ 抽屉  │   │ 新窗口 │   │ Confirm  │         │
│   │/页面  │   │/页面  │   │ 详情页 │   │ 二次确认 │         │
│   └───┬───┘   └───┬───┘   └───────┘   └────┬─────┘         │
│       │           │                        │               │
│       ▼           ▼                        ▼               │
│   提交成功     提交成功                 确认删除            │
│       │           │                        │               │
│       └───────────┴────────────────────────┘               │
│                   │                                         │
│                   ▼                                         │
│            刷新列表数据                                      │
│            (不能整页刷新)                                    │
└─────────────────────────────────────────────────────────────┘
```