---
alwaysApply: true
description: 前端布局规范
---

# 前端布局与页面结构规范

本文档整合列表页、抽屉页、详情页的页面结构与交互规范，统一前端页面布局标准。

## 一、通用原则

- 页面布局需保持信息层级清晰，优先采用“导航在上、内容在下、操作就近”的结构。
- 同类页面保持一致的组件组织方式与交互路径。
- 仅刷新数据，不通过整页刷新处理交互结果。
- 页面需考虑加载态、空态、失败态、无权限、网络异常等边界状态。
- 在移动端需保证可用性：可读、可点、可滚动。

## 二、列表页规范

### 2.1 页面结构

- 列表页采用垂直结构：
  - 面包屑导航区
  - 筛选器区
  - 表格区
- 标准路径格式为“首页 > 当前列表页名称”。
- 页面结构需体现一致性、易用性、可维护性。

### 2.2 面包屑要求

- 当前页（最后一项）不可点击。
- 前序项可跳转到对应页面。
- 建议使用紧凑尺寸，减少顶部空间占用。

### 2.3 筛选器要求

- 筛选器作为统一查询入口，需包含搜索与重置能力。
- 必须使用 `Filter` 作为容器、`FilterItem` 作为筛选项单元。
- 筛选器内的输入与操作组件（如 Input、Select、DatePicker、Button）必须来自 YAIM 组件体系。
- 布局建议每行放置 2-4 个筛选项，并支持展开/收起。
- 重置后应恢复默认查询条件并重新加载列表数据。

### 2.4 表格要求

- 默认启用分页，数据结构应包含内容与分页字段（如 content、total、page、size）。
- 操作列默认放第一列；单条数据操作（编辑、删除等）放操作列。
- 新增等全局操作放工具栏区域，不放在表格行内。
- 表格内文本与状态展示应统一使用 `Text` 组件体系。
- 全屏能力默认开启；页面场景简单时可按需关闭。

### 2.5 交互与批量操作

- 新增/编辑默认打开抽屉完成录入与修改。
- 编辑场景必须透传当前记录 `id` 到抽屉，用于数据回填与详情获取。
- 删除操作必须二次确认，删除成功后刷新列表数据。
- 删除按钮应使用 `link` 形态，并通过 `needConfirm` 与 `confirmConfig` 配置确认弹层。
- 批量操作按需开启行选择能力，可配置本页全选与全量选择策略。
- 批量选择需明确以下配置策略：
  - `isShowBatchSelection`：是否展示批量勾选能力
  - `enableCrossPageSelection`：是否支持跨页勾选
  - `selectedFirst`：是否默认选中首行
- 涉及批量跨页选择时，需与后端能力保持一致。

## 三、抽屉页规范

### 3.1 适用场景

- 适用于表单编辑、数据录入、复杂配置等需要保留上下文的场景。
- 相比弹窗，抽屉更适合字段较多、流程较长的操作。

### 3.2 页面结构

- 抽屉由四部分组成：
  - 容器区（侧滑出现）
  - 头部区（标题、关闭）
  - 内容区（表单与分组）
  - 底部操作区（提交、取消、重置）
- 关闭方式需覆盖：右上角关闭、遮罩关闭、ESC、底部取消。

### 3.3 宽度与布局

- 抽屉宽度建议按复杂度分档：500、800、1200。
- 列布局建议：
  - 500：单列
  - 800：双列
  - 1200：三列
- 分组内容可使用卡片或分段标题增强可读性。

### 3.4 表单与交互

- 表单控件推荐映射如下：

| 场景 | 推荐组件 |
|------|----------|
| 简短文本 | Input |
| 长文本 | TextArea |
| 单选项 | Select 或 Radio |
| 多选项 | Checkbox |
| 布尔开关 | Switch |
| 日期时间 | DatePicker |
| 层级选择 | Cascader |

- 表单需提供实时验证与明确错误反馈。
- 提交流程需包含：校验、提交、成功提示、关闭、上层数据刷新。
- 编辑场景需支持数据回填；新增场景需提供合理默认值。
- 抽屉关闭时需考虑未保存数据处理策略（确认离开或草稿机制）。

### 3.5 响应式与性能

- 小屏下抽屉应提高占屏比例，表单项改为垂直堆叠。
- 操作按钮在移动端需保证触控可用性。
- 大量内容场景应考虑懒加载、列表优化与输入防抖。

## 四、详情页规范

### 4.1 适用场景

- 用于集中展示单条记录完整信息，如用户、订单、产品详情。

### 4.2 页面结构

- 详情页建议结构：
  - 面包屑导航
  - 标题与操作区
  - 主信息展示区
  - 关联数据区（可选）
  - 补充说明区（可选）
- 面包屑推荐层级格式：`首页 > 一级菜单 > 列表页面 > 当前详情`。
- 详情信息区建议按 2-3 列组织字段，保持标签和值对齐。

### 4.3 关联数据区

- 关联列表可放页面中下部，与主信息区分隔清晰。
- 当存在多个关联模块时，每个模块应有独立小标题。
- 功能较轻的关联表格可关闭不必要控件（如全屏、分页）。

### 4.4 交互要求

- 常用操作（编辑、返回）放在标题区域右侧。
- 详情加载阶段应有全页加载态，避免内容跳动。
- 失败场景需有重试与提示。

### 4.5 视觉与响应式

- 推荐间距：
  - 页面内边距约 20
  - 模块间距 8-24
  - 标题与内容间距约 8
- 推荐色值：
  - 标题文本：#333333
  - 成功状态：#52c41a
  - 危险状态：#ff4d4f
- 窄屏下标题区允许换行，信息区改为单列，关联表格支持横向滚动。

## 五、规则落地检查清单

- 列表页是否为“面包屑 + 筛选器 + 表格”标准结构。
- 新增/编辑是否通过抽屉承载并在成功后刷新列表。
- 删除是否包含二次确认并在成功后刷新列表。
- 抽屉是否具备完整关闭方式、校验反馈、回填能力。
- 详情页是否具备“标题操作 + 信息分组 + 关联模块”的清晰层级。
- 是否覆盖加载、空态、失败、权限、网络异常等边界场景。
